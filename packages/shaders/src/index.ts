/**
 * @synapse/shaders
 *
 * Public entry point for the Shaders package.
 * Intentionally empty in Phase 0 (foundation only). Shader registry, library,
 * and compilation are implemented in later phases.
 */

export {};
