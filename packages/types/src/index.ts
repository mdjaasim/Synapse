/**
 * @synapse/types
 *
 * Public entry point for the Types package.
 * Intentionally empty in Phase 0 (foundation only). Global type definitions,
 * enums, interfaces, and event types are implemented in later phases.
 */

export {};
