/**
 * @synapse/materials
 *
 * Public entry point for the Materials package.
 * Intentionally empty in Phase 0 (foundation only). Material tokens, presets,
 * and the material factory are implemented in later phases.
 */

export {};
