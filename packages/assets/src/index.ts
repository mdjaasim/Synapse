/**
 * @synapse/assets
 *
 * Public entry point for the Assets package.
 * Intentionally empty in Phase 0 (foundation only). Asset registry, streaming,
 * and loading are implemented in later phases.
 */

export {};
