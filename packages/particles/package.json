{
  "name": "@synapse/particles",
  "version": "0.0.0",
  "private": true,
  "description": "Particles — particle systems, behaviors, presets, simulation, GPU effects.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  }
}
