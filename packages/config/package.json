{
  "name": "@synapse/config",
  "version": "0.0.0",
  "private": true,
  "description": "Config — constants, feature flags, environment, build configuration, theme tokens.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  }
}
