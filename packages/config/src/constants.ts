/**
 * Application-agnostic constants for the SYNAPSE experience.
 */

export const SITE = {
  name: "PROJECT SYNAPSE",
  description: "An award-level interactive portfolio experience.",
} as const;

/** localStorage key prefix for persisted client state. */
export const STORAGE_PREFIX = "synapse";
