{
  "name": "@synapse/camera",
  "version": "0.0.0",
  "private": true,
  "description": "Camera — controller, rigs, spline paths, lens profiles, camera state machine.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@synapse/types": "workspace:*",
    "gsap": "^3.12.7",
    "zustand": "^5.0.14"
  }
}
