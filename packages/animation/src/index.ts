/**
 * @synapse/animation
 *
 * Public entry point for the Animation package.
 * Intentionally empty in Phase 0 (foundation only). GSAP timelines, the
 * animation registry, and motion tokens are implemented in the animation phase.
 */

export {};
