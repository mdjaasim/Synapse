{
  "name": "@synapse/animation",
  "version": "0.0.0",
  "private": true,
  "description": "Animation — GSAP timelines, timeline mapping, animation registry, motion tokens, transition library.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@synapse/config": "workspace:*",
    "@synapse/types": "workspace:*",
    "gsap": "^3.13.0"
  }
}
