"use client";

/**
 * Custom cursor slot. The interactions phase moves real cursor rendering into
 * @synapse/interactions and wires it to the interaction store.
 */
export function Cursor() {
  return null;
}
